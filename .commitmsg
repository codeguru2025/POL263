feat: comprehensive policy report, commission enhancements, and bug fixes

- Merge spreadsheet format into Policy Report with cover amount, beneficiary,
  dependent, and member number columns (UI + CSV export)
- Add product-version commission config (first months, recurring, clawback,
  funeral incentive) with edit support
- Implement commission clawback on policy lapse/cancel and rollback on reinstate
- Add comprehensive receipts report tab with receipt/payment branch columns
- Fix covered lives dashboard to count actual policy members

Bug fixes:
- Security: add requirePermission to report export endpoint
- Fix duplicate rows from policyMembers leftJoin causing inflated reports
- Fix voided receipts counted in finance totals
- Fix date comparison format mismatch in receipt aggregates
- Fix graceDaysUsed going negative when grace extended
- Fix double ? in query strings breaking report filters
- Fix query keys missing filter deps causing stale cached data
- Remove duplicate GET /api/add-ons route registration
- Fix EditVersionDialog missing reinstatement fields (data loss on edit)
- Fix net commission total losing negative sign
- Add missing policy transitions (inactive->active, grace->active, lapsed->active)
- Fix commission gap between first months and recurring start
- Fix null checks on receivedAt dates and parseFloat guards
- Fix Set iteration TS2802 errors with Array.from()
