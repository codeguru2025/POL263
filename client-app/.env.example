# Copy to .env and fill in real values. Never commit .env.

# API_BASE for the app's fetch client (src/config.ts) — falls back to a LAN dev address if unset.
EXPO_PUBLIC_API_BASE=http://192.168.9.138:5000
